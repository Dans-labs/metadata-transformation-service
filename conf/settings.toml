[default]
#FastAPI
fastapi_title = "Metadata Transformation Service"
fastapi_description = "This service supports metadata transformation across various formats and schemas, including XSL, Jinja templates, and JSON path mappings, enabling interoperability between different metadata standards."

temp_transform_file = "/tmp/transform"
#logs
otlp_enable = false
log_file = "@format {env[BASE_DIR]}/logs/mts.log"
log_level = 10
log_format = "%(asctime)s %(levelname)s %(name)s %(threadName)s : %(message)s"
#Note: below the list of logging level
#CRITICAL = 50
#ERROR = 40
#WARNING = 30
#INFO = 20
#DEBUG = 10
#xsl location
saved_xslt_dir = "@format {env[BASE_DIR]}/resources/saved-xsl"

#jinja template location
jinja_and_prop_dir = "@format {env[BASE_DIR]}/resources/saved-templates"

[development]

[staging]

[testing]

[production]